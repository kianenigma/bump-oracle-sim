## Future Research

1. We need a short price path that contains all of the following:

* For each "drop" and "increase" in price of 10%, 30%, 70%
	* one variation where venues are in sync and just have jitter
		* one variation where price recovers to 20% of the original
		* one variation where price recovers to 50% of the original
		* one variation where price recovers to 90% of the original
	* one variation where venues diverge with 10x the normal jitter
* some period of mild volatility in between with venues in sync within normal jitter
* real price is still MEAN of all venues (un weighted)

This should be a new parameter in the CLI called `----data-source synthetic` that will do this, and it will reject --from and so on.

---

Then we need to refactor all our malicious validators to be applicable to either the median model or the nudge.
A lot of them are doing the same thing in the nudge model, because they are realistically only relevant to the median model.

Each malicious validator has a associated property that is checked in the engine. It is either applicable to a nudge run, or a median run. If worngly configured, the run will fail.

We also need to keep in the code, but exclude from scenarios all malicious validators that are targeted towards defeating the confidence tracking. They are not relevant anymore.

The new scenario will:

* Run mean (with no confidence tracking) and nudge model against only those malicious actors that are relevant
	* While we mostly focus on 33% malicious (+ honest), also run 10% as an extra data-point
	* we will run with auto epsilon, epsilon/2 and epsilon/4 as we do now.

---

So far our conclusion is that the median oracle aggregator is strictly better than the bump/nudge model. Our goal is to attack the system, and break that assumption.

We have previously worked on some attacks that required us to implement a confidence tracking system, and made attackers that try and avoid getting caught in this system. This is not our goal now. Our goal is to keep the median model simple, without any confidence tracking.

We are only focusing on new attackers against the median model.

For at least 10 rounds, similar to what we did in @TOURNAMENT.md file, spawn a sub-agent that will try and attack the system. The subagent will likely write a new attacker malicious validator. Then simulate this malicious validator by adding it to our `validate-median` scenario, and see how it behaves. The terminal logs of this scenario should be self-explanatory

For each new attacker, report:

* How worse it made median model perform
* Did it make it worse than nudge in any metric?
